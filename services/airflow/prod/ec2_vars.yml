---
ami_name: "base-ubuntu18-py38*"
instances:
  - name: "p-airflow-web-01"
    instance_type: t3a.medium
    instance_role: payments-batch-submission-feedback-role
    instance_tags:
      gunicorn: true

instance_group_name: apfm_prod
base_tags:
  "Env": "prod"
  "Service": "airflow"
  "Team": "MLE"

root_volume_size: 64
additional_volume_size: 0
is_public_ip_needed: false
additional_volume_needed: false
is_mounting_volume_needed: false
nginx_needed: true
add_deployment_cron: true

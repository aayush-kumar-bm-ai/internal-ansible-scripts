- type: log
  processors:
    - add_fields:
        target: ""
        fields:
          log_type: "nginx-err"
  enabled: true
  multiline.pattern: "^[0-9]{4}/[0-9]{2}/[0-9]{2}"
  multiline.negate: true
  multiline.match: after
  paths:
    - "{{ nginx_error_logs_path }}"